const styles = {
    Select: {
        width: 250,
        height: 48,
        borderRadius: "8px",
        fontSize: "15px",
        backgroundColor: "#fff",
        "& .MuiSelect-select": {
            display: "flex",
            alignItems: "center",
            height: "48px"
        }
    }
}

export default styles